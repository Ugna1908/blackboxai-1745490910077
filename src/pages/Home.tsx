import React from 'react';
import Homepage from '../components/Homepage/Homepage';

const Home: React.FC = () => {
    return (
        <div>
            <Homepage />
        </div>
    );
};

export default Home;